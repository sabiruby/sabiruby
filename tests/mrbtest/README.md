`src/*.rb` are copied from mruby 4.1.0-rc2 `test/assert.rb` and `test/t/` (MIT, Copyright (c) 2010- mruby developers)
and `*.mrb` are their compilation by the reference `mrbc`. Regenerate with `tools/mrbtest.sh`.
`baseline.txt` is the per-file `ok` floor checked by `cargo test`.
